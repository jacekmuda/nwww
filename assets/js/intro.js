function intro_start(intro_content) {
    if (!intro_content) {
        return;
    } else {
        const photos = intro_content.photos,
            lead = intro_content.lead;
        console.log(photos)

    }
}

intro_start();